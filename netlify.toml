# netlify.toml — FINAL WORKING VERSION (staff app)
[build]
  publish = "build"
  functions = "netlify/functions"

# Remove the broken redirect — Netlify adds this automatically for functions
# DO NOT include any [[redirects]] with /.netlify/functions

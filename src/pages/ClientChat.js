import { Container, Typography } from '@mui/material';

export default function ClientChat() {
  return (
    <Container sx={{ mt: 4 }}>
      <Typography variant="h5" sx={{ color: '#C41E3A' }}>
        Client Messages — coming soon
      </Typography>
    </Container>
  );
}

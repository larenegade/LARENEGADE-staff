import { Container, Typography } from '@mui/material';

export default function AIAssistant() {
  return (
    <Container sx={{ mt: 4 }}>
      <Typography variant="h5" sx={{ color: '#C41E3A' }}>
        AI Assistant — coming soon
      </Typography>
    </Container>
  );
}
